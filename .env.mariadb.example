# Env del contenedor `mariadb` en docker-compose (servidor). Copiar a .env.mariadb (no versionado).

MARIADB_DATABASE=funeraria_minaya
MARIADB_USER=funeraria_app
MARIADB_PASSWORD=cambia-esta-clave
MARIADB_ROOT_PASSWORD=cambia-esta-clave-root
